if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# Propmt at the bottom
# printf '\n%.0s' {1..100}
# Fast fetch
# fastfetch
# echo ""
# echo ""
# echo ""

export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="powerlevel10k/powerlevel10k"
plugins=(
	git
	zsh-syntax-highlighting
	zsh-autosuggestions
)
source $ZSH/oh-my-zsh.sh

# ENV
export NB_MANAGEMENT_UR="https://vpn.apsolutions.ru:443"
export MY_LOCAL_IP=192.168.135.198

# Aliases
alias yazi-backup="ya pkg list > ~/.config/yazi/yazi-packages.txt"
alias ls="lsd"
alias cat="bat"
# alias vim="nvim"
alias chcd='cd $(chezmoi source-path)'
alias battery='cat /sys/class/power_supply/BAT*/capacity'
alias dtable='docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"'
alias dports='docker ps --format "{{.Names}}\n\tPorts: {{.Ports}}"'
alias dinfo='docker ps --format "{{.Names}}\n\tContainer ID: {{.ID}}\n\tCommand: {{.Command}}\n\tImage: {{.Image}}\n\tCreatedAt: {{.CreatedAt}}\n\tStatus: {{.Status}}\n\tPorts: {{.Ports}}"'





[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh



[[ -f /home/krainov/.config/.dart-cli-completion/zsh-config.zsh ]] && . /home/krainov/.config/.dart-cli-completion/zsh-config.zsh || true
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

export PATH="$PATH:$HOME/.cargo/bin"
export PATH=$PATH:$(go env GOPATH)/bin
export PATH=$PATH:$HOME/.local/bin

# [[ -s "/home/krainov/.gvm/scripts/gvm" ]] && source "/home/krainov/.gvm/scripts/gvm"

# pnpm
export PNPM_HOME="/home/krainov/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end

